class Rules {
    constructor() {
        this.rules = [];
    }

    addRule(rule) {
        // @TODO: Does the rule already exist? Can we have duplicate rules?
        // @TODO: Validate the rule. It needs a name and a rule. Should that be called a `definition`?
        // @TODO: Add the rule to the list.
    }
}